node_modules
# Keep environment variables out of version control
.env

/generated/prisma
dist/
-w
*.csv
# Config del agente persistido en runtime (no versionar)
agent-config.json
tsconfig.build.tsbuildinfo
